'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {api} from '@/lib/api';
type C={name:string;industry:string;contactName?:string;contactEmail?:string;website?:string;timezone:string;marketingGoals?:string;status:string};export default function Detail({params}:{params:Promise<{id:string}>}){const router=useRouter(),[id,setId]=useState(''),[c,setC]=useState<C|null>(null),[edit,setEdit]=useState(false);useEffect(()=>{params.then(p=>{setId(p.id);api<C>(`/clients/${p.id}`).then(setC)})},[params]);if(!c)return <main className="main">Loading…</main>;async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const updated=await api<C>(`/clients/${id}`,{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});setC(updated);setEdit(false)}async function archive(){await api(`/clients/${id}/archive`,{method:'POST'});router.push('/clients')}return <main className="main"><Link className="link" href="/clients">← All clients</Link><div className="modal-card">{edit?<form className="form-grid" onSubmit={save}>{[['name','Business name'],['industry','Industry'],['contactName','Contact name'],['contactEmail','Contact email'],['website','Website'],['timezone','Timezone']].map(([n,l])=><div className="field" key={n}><label>{l}</label><input name={n} defaultValue={String(c[n as keyof C]||'')} required={n==='name'||n==='industry'}/></div>)}<div className="field wide"><label>Marketing goals</label><textarea name="marketingGoals" defaultValue={c.marketingGoals}/></div><button className="btn btn-primary">Save changes</button></form>:<><span className="badge">{c.status}</span><h1>{c.name}</h1><p className="muted">{c.industry} · {c.timezone}</p><h3>Contact</h3><p>{c.contactName||'Not added'}<br/>{c.contactEmail}</p><h3>Website</h3><p>{c.website||'Not added'}</p><h3>Marketing goals</h3><p>{c.marketingGoals||'No goals added yet.'}</p><div className="actions"><button className="btn btn-primary" onClick={()=>setEdit(true)}>Edit client</button><button className="btn btn-secondary" onClick={archive}>Archive</button></div></>}</div></main>}
