'use client';import Link from 'next/link';import {FormEvent,useEffect,useState} from 'react';import {api} from '@/lib/api';
type Kit={primaryColor:string;secondaryColor:string;accentColor:string;headingFont:string;bodyFont:string;toneOfVoice?:string;targetAudience?:string;brandStory?:string;products:string[];services:string[]};const empty:Kit={primaryColor:'#176b4b',secondaryColor:'#dfff9e',accentColor:'#13201c',headingFont:'Inter',bodyFont:'Inter',products:[],services:[]};
export default function BrandKit({params}:{params:Promise<{id:string}>}){const[id,setId]=useState(''),[kit,setKit]=useState(empty),[saved,setSaved]=useState(false);useEffect(()=>{params.then(p=>{setId(p.id);api<Kit|null>(`/clients/${p.id}/brand-kit`).then(k=>k&&setKit(k))})},[params]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const v=Object.fromEntries(new FormData(e.currentTarget));const data={...v,products:String(v.products||'').split('\n').filter(Boolean),services:String(v.services||'').split('\n').filter(Boolean)};setKit(await api<Kit>(`/clients/${id}/brand-kit`,{method:'PUT',body:JSON.stringify(data)}));setSaved(true)}return <main className="main"><Link className="link" href={`/clients/${id}`}>← Client profile</Link><header className="topbar"><div><div className="eyebrow" style={{color:'var(--green)'}}>Brand intelligence</div><h1>Brand Kit</h1><p className="muted">Give the AI consistent creative direction.</p></div></header>{saved&&<div className="alert alert-success">Brand Kit saved.</div>}<form className="modal-card form-grid" onSubmit={submit}><div className="field"><label>Primary color</label><input name="primaryColor" type="color" defaultValue={kit.primaryColor}/></div><div className="field"><label>Secondary color</label><input name="secondaryColor" type="color" defaultValue={kit.secondaryColor}/></div><div className="field"><label>Accent color</label><input name="accentColor" type="color" defaultValue={kit.accentColor}/></div><div className="field"><label>Heading font</label><input name="headingFont" defaultValue={kit.headingFont}/></div><div className="field"><label>Body font</label><input name="bodyFont" defaultValue={kit.bodyFont}/></div><div className="field wide"><label>Tone of voice</label><textarea name="toneOfVoice" defaultValue={kit.toneOfVoice}/></div><div className="field wide"><label>Target audience</label><textarea name="targetAudience" defaultValue={kit.targetAudience}/></div><div className="field wide"><label>Brand story</label><textarea name="brandStory" defaultValue={kit.brandStory}/></div><div className="field"><label>Products (one per line)</label><textarea name="products" defaultValue={kit.products.join('\n')}/></div><div className="field"><label>Services (one per line)</label><textarea name="services" defaultValue={kit.services.join('\n')}/></div><button className="btn btn-primary">Save Brand Kit</button></form></main>}
