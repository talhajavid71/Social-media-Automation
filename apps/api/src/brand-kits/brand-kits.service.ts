import { Injectable,NotFoundException } from '@nestjs/common';import { PrismaService } from '../prisma/prisma.service';import { BrandKitDto } from './brand-kit.dto';
@Injectable() export class BrandKitsService{constructor(private db:PrismaService){}async owns(ownerId:string,clientId:string){if(!await this.db.client.findFirst({where:{id:clientId,ownerId}}))throw new NotFoundException('Client not found.')}async get(ownerId:string,clientId:string){await this.owns(ownerId,clientId);return this.db.brandKit.findUnique({where:{clientId}})}async save(ownerId:string,clientId:string,d:BrandKitDto){await this.owns(ownerId,clientId);return this.db.brandKit.upsert({where:{clientId},create:{...d,clientId},update:d})}}
